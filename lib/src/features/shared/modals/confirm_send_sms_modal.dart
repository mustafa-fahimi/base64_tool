import 'package:flutter/material.dart';

class ConfirmSendSmsModal extends StatelessWidget {
  const ConfirmSendSmsModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
